> Current demo branch, deployment and priorities: [PROJECT_STATUS.md](PROJECT_STATUS.md). The record below is historical context; it is not the current demo release sign-off.

# Deployment Branch Confirmation

The production branch for `shop.millco.in` cannot be confirmed from local repository files alone.

Before any production rollout, confirm the following in the hosting provider dashboard:

- Production project name.
- Git repository URL.
- Production branch.
- Build command.
- Environment variables source.
- Current production deployment commit SHA.
- Preview/staging deployment availability.

Record the result here when confirmed:

- Hosting provider:
- Production project:
- Repository:
- Production branch:
- Current production commit:
- Preview deployment URL:
- Confirmed by:
- Confirmed on:

Until this is confirmed, local work should stay on hardening branches and should not be assumed to affect production.

